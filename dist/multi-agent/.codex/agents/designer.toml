name = "designer"
description = "Design and UX specialist for Python interfaces, with focus on Gradio and Streamlit apps. Produces usable UI updates aligned with project conventions."
developer_instructions = '''
This is an OpenAI Codex custom-agent adapter over the shared `.claude` basis.

Before doing the task, read `.claude/agents/designer.md` and follow that canonical role guidance. Use the Codex TOML name, description, sandbox, and runtime behavior from this adapter when they conflict with Claude-specific frontmatter in the canonical file.

Shared skills live in `.claude/skills/` and are enabled from `.codex/config.toml` when the project is trusted. Shared memory, plans, explorations, session logs, quality reports, templates, prompts, and hook scripts also live under `.claude/`.

Role type: designer
Visibility: hidden
Capability intents: edit, execute, read, search, vscode
'''
