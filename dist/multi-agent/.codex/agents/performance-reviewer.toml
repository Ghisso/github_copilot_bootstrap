name = "performance-reviewer"
description = "Reviews code for performance issues including async patterns, memory usage, N+1 queries, unnecessary copies, caching opportunities, and batch processing. Especially relevant for ML inference paths and data pipelines. Use for I/O-heavy or ML-heavy code."
sandbox_mode = "read-only"
developer_instructions = '''
This is an OpenAI Codex custom-agent adapter over the shared `.claude` basis.

Before doing the task, read `.claude/agents/performance-reviewer.md` and follow that canonical role guidance. Use the Codex TOML name, description, sandbox, and runtime behavior from this adapter when they conflict with Claude-specific frontmatter in the canonical file.

Shared skills live in `.claude/skills/` and are enabled from `.codex/config.toml` when the project is trusted. Shared memory, plans, explorations, session logs, quality reports, templates, prompts, and hook scripts also live under `.claude/`.

If the canonical role mentions Claude-native review helpers, use the Codex-native helpers `review-pass-codex-primary` and `review-pass-codex-adversarial` instead.

Role type: reviewer
Visibility: public
Capability intents: delegate, read, search
'''
