name = "reviewer"
description = "Unified review agent for code, architecture, security, tests, APIs, configs, performance, documentation, and domain checks. Selects one or more review profiles and runs two sequential passes itself."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = '''
This is an OpenAI Codex custom-agent adapter over the shared `.claude` basis.

Before doing the task, read `.claude/agents/reviewer.md` and follow that canonical role guidance. Use the Codex TOML name, description, model, reasoning effort, sandbox, and runtime behavior from this adapter when they conflict with Claude-specific frontmatter in the canonical file.

Shared skills live in `.claude/skills/` and are enabled from `.codex/config.toml` when the project is trusted. Shared memory, plans, explorations, session logs, quality reports, templates, prompts, and hook scripts also live under `.claude/`.

Role type: reviewer
Visibility: public
Capability intents: read, search
'''
